version: '3.8'

services:
  # Development Environment - React App
  react-app:
    image: alexalphonse/devops-build-dev:latest
    ports:
      - "80:80"
    container_name: react-app
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
